# Personal Website

Sam Gray's personal portfolio site. Plain HTML and CSS, no build step.

## Structure

- `index.html` — landing page
- `style.css` — styles

## Hosting

Deployed via **GitHub Pages** from the `main` branch root.

To migrate to Netlify: connect this repo on netlify.com — `netlify.toml` is already configured.
